---
title: Dialog
description: Modal dialog components with Alpine.js state management, keyboard shortcuts, and flexible positioning
category: overlay
order: 1
---

import Dialog from '@/components/ui/overlay/dialog/Dialog.astro';
import DialogContent from '@/components/ui/overlay/dialog/DialogContent.astro';
import DialogHeader from '@/components/ui/overlay/dialog/DialogHeader.astro';
import DialogFooter from '@/components/ui/overlay/dialog/DialogFooter.astro';
import DialogTrigger from '@/components/ui/overlay/dialog/DialogTrigger.astro';
import { Button } from '@/components/ui/forms';
import Code from '@/components/ui/display/Code.astro';
import Tabs from '@/components/ui/navigation/tabs/Tabs.astro';
import TabsList from '@/components/ui/navigation/tabs/TabsList.astro';
import TabsTrigger from '@/components/ui/navigation/tabs/TabsTrigger.astro';
import TabsContent from '@/components/ui/navigation/tabs/TabsContent.astro';

## Overview

Modal dialog components with Alpine.js state management, keyboard shortcuts, auto-focus, and flexible positioning variants. Includes support for forms, confirmations, and side panels with proper accessibility features.

<Tabs defaultValue="preview" class="h-auto">
    <TabsList variant="pills" slot="list">
        <TabsTrigger value="preview" variant="pills">Preview</TabsTrigger>
        <TabsTrigger value="code" variant="pills">Code</TabsTrigger>
    </TabsList>
    <TabsContent value="preview" preview class="space-y-8">
        <div class="space-y-4">
            <div class="flex gap-4">
                <Dialog>
                    <DialogTrigger variant="outline">
                        Open Dialog
                    </DialogTrigger>
                    <DialogContent variant="center">
                        <DialogHeader>
                            <h3 class="text-lg font-semibold">Dialog Title</h3>
                            <p class="text-sm text-muted-foreground">
                                Dialog description or subtitle
                            </p>
                        </DialogHeader>
                        <div class="py-4">
                            <p class="text-sm">
                                This is the main content area. You can put any content here including forms, text, or other components.
                            </p>
                        </div>
                        <DialogFooter>
                            <Button variant="outline" x-on:click="close()">Cancel</Button>
                            <Button variant="default">Save Changes</Button>
                        </DialogFooter>
                    </DialogContent>
                </Dialog>

                {/* Position Variants */}
                <Dialog>
                    <DialogTrigger variant="outline">
                        Left Panel
                    </DialogTrigger>
                    <DialogContent variant="left">
                        <DialogHeader>
                            <h4 class="font-medium">Left Sidebar</h4>
                        </DialogHeader>
                        <div class="py-4  h-full">
                            <p class="text-sm">Content slides in from the left.</p>
                        </div>
                        <DialogFooter>
                            <Button x-on:click="close()">Close</Button>
                        </DialogFooter>
                    </DialogContent>
                </Dialog>

                <Dialog>
                    <DialogTrigger variant="outline">
                        Right Panel
                    </DialogTrigger>
                    <DialogContent variant="right">
                        <DialogHeader>
                            <h4 class="font-medium">Right Sidebar</h4>
                        </DialogHeader>
                        <div class="py-4 h-full">
                            <p class="text-sm">Content slides in from the right.</p>
                        </div>
                        <DialogFooter>
                            <Button x-on:click="close()">Close</Button>
                        </DialogFooter>
                    </DialogContent>
                </Dialog>

                <Dialog>
                    <DialogTrigger variant="outline">
                        Top Banner
                    </DialogTrigger>
                    <DialogContent variant="top">
                        <DialogHeader>
                            <h4 class="font-medium">Top Banner</h4>
                        </DialogHeader>
                        <div class="py-4">
                            <p class="text-sm">Content slides down from the top.</p>
                        </div>
                        <DialogFooter>
                            <Button variant="outline" x-on:click="close()">Close</Button>
                        </DialogFooter>
                    </DialogContent>
                </Dialog>

                <Dialog>
                    <DialogTrigger variant="outline">
                        Bottom Sheet
                    </DialogTrigger>
                    <DialogContent variant="bottom">
                        <DialogHeader>
                            <h4 class="font-medium">Bottom Sheet</h4>
                        </DialogHeader>
                        <div class="py-4">
                            <p class="text-sm">Content slides up from the bottom.</p>
                        </div>
                        <DialogFooter>
                            <Button variant="outline" x-on:click="close()">Close</Button>
                        </DialogFooter>
                    </DialogContent>
                </Dialog>
            </div>
        </div>

    </TabsContent>
    <TabsContent value="code">

<Code
    language="astro"
    code={`---
import Dialog from '@/components/ui/overlay/dialog/Dialog.astro';
import DialogContent from '@/components/ui/overlay/dialog/DialogContent.astro';
import DialogHeader from '@/components/ui/overlay/dialog/DialogHeader.astro';
import DialogFooter from '@/components/ui/overlay/dialog/DialogFooter.astro';
import DialogTrigger from '@/components/ui/overlay/dialog/DialogTrigger.astro';
import { Button } from '@/components/ui/forms';
---

{/* Basic Modal Dialog */}

<Dialog>
    <DialogTrigger variant="button">Open Dialog</DialogTrigger>
    <DialogContent variant="center">
        <DialogHeader>
            <h3 class="text-lg font-semibold">Dialog Title</h3>
            <p class="text-sm text-muted-foreground">Dialog description</p>
        </DialogHeader>
        <div class="py-4">
            <p>Your dialog content goes here.</p>
        </div>
        <DialogFooter>
            <Button variant="outline" x-on:click="close()">
                Cancel
            </Button>
            <Button variant="default">Confirm</Button>
        </DialogFooter>
    </DialogContent>
</Dialog>

{/* Side Panel Dialog */}

<Dialog>
    <DialogTrigger variant="outline">Settings</DialogTrigger>
    <DialogContent variant="right">
        <DialogHeader>
            <h4 class="font-medium">Settings</h4>
        </DialogHeader>
        <div class="py-4 space-y-4">
            <div>
                <label class="text-sm font-medium">Theme</label>
                <select class="w-full mt-1 px-3 py-2 border rounded-md">
                    <option>Light</option>
                    <option>Dark</option>
                    <option>Auto</option>
                </select>
            </div>
        </div>
        <DialogFooter>
            <Button variant="default" x-on:click="close()">
                Save
            </Button>
        </DialogFooter>
    </DialogContent>
</Dialog>

{/* Form Dialog with Auto-Focus */}

<Dialog autoFocus={true}>
    <DialogTrigger variant="button">Add User</DialogTrigger>
    <DialogContent variant="center">
        <DialogHeader>
            <h3 class="text-lg font-semibold">Add New User</h3>
        </DialogHeader>
        <div class="py-4 space-y-4" x-data="{ name: '', email: '' }">
            <div class="space-y-2">
                <label class="text-sm font-medium">Name</label>
                <input
                    type="text"
                    x-model="name"
                    placeholder="Enter name"
                    class="w-full px-3 py-2 border rounded-md focus:ring-2 focus:ring-ring"
                />
            </div>
            <div class="space-y-2">
                <label class="text-sm font-medium">Email</label>
                <input
                    type="email"
                    x-model="email"
                    placeholder="Enter email"
                    class="w-full px-3 py-2 border rounded-md focus:ring-2 focus:ring-ring"
                />
            </div>
        </div>
        <DialogFooter>
            <Button variant="outline" x-on:click="close()">
                Cancel
            </Button>
            <Button variant="default" x-on:click="close()">
                Add User
            </Button>
        </DialogFooter>
    </DialogContent>
</Dialog>

{/* Keyboard Shortcut Dialog */}

<Dialog keyboardShortcut="cmd+k">
    <DialogTrigger variant="ghost">Command Palette (⌘K)</DialogTrigger>
    <DialogContent variant="center">
        <DialogHeader>
            <h3 class="text-lg font-semibold">Command Palette</h3>
        </DialogHeader>
        <div class="py-4">
            <input
                type="text"
                placeholder="Type a command..."
                class="w-full px-3 py-2 border rounded-md"
            />
        </div>
    </DialogContent>
</Dialog>
`} />
    </TabsContent>
</Tabs>

## API Reference

### Dialog

Main container with state management and keyboard handling.

| Prop               | Type      | Default                                            | Description                                      |
| ------------------ | --------- | -------------------------------------------------- | ------------------------------------------------ |
| `closeable`        | `boolean` | `true`                                             | Whether dialog can be closed with Escape key     |
| `lockScroll`       | `boolean` | `true`                                             | Prevent body scrolling when dialog is open       |
| `keyboardShortcut` | `string`  | -                                                  | Keyboard shortcut to open dialog (e.g., "cmd+k") |
| `autoFocus`        | `boolean` | `false`                                            | Auto-focus first input/button when opened        |
| `focusSelector`    | `string`  | `'input, button, [tabindex]:not([tabindex="-1"])'` | CSS selector for auto-focus element              |

### DialogContent

Content wrapper with backdrop, animations, and positioning variants.

| Prop       | Type                                                                 | Default    | Description        |
| ---------- | -------------------------------------------------------------------- | ---------- | ------------------ |
| `variant`  | `'center' \, 'left' \, 'right' \, 'top' \, 'bottom' \, 'fullscreen'` | `'center'` | Dialog positioning |
| `backdrop` | `'dark' \, 'light' \, 'blur' \, 'none'`                              | `'dark'`   | Backdrop style     |

### DialogHeader

Header section with optional close button and title content.

| Prop        | Type      | Default | Description                           |
| ----------- | --------- | ------- | ------------------------------------- |
| `closeable` | `boolean` | `true`  | Show close button in top-right corner |

### DialogFooter

Footer section with flexible button layouts.

| Prop      | Type                                        | Default | Description             |
| --------- | ------------------------------------------- | ------- | ----------------------- |
| `justify` | `'start' \, 'end' \, 'center' \, 'between'` | `'end'` | Button alignment        |
| `gap`     | `'sm' \, 'md' \, 'lg'`                      | `'sm'`  | Spacing between buttons |

### DialogTrigger

Trigger element that opens the dialog when clicked.

| Prop      | Type                                            | Default     | Description  |
| --------- | ----------------------------------------------- | ----------- | ------------ |
| `variant` | `'default' \, 'button' \, 'outline' \, 'ghost'` | `'default'` | Visual style |
