---
title: Spacer
description: Flexible space component that grows to fill available space in flex layouts
category: layout
order: 9
---

import Spacer from '@/components/ui/layout/Spacer.astro';
import { Row, Column } from '@/components/ui/layout';
import { Button } from '@/components/ui/forms';
import Code from '@/components/ui/display/Code.astro';
import Tabs from '@/components/ui/navigation/tabs/Tabs.astro';
import TabsList from '@/components/ui/navigation/tabs/TabsList.astro';
import TabsTrigger from '@/components/ui/navigation/tabs/TabsTrigger.astro';
import TabsContent from '@/components/ui/navigation/tabs/TabsContent.astro';

## Overview

The Spacer component is a flexible utility that grows to fill available space in flex layouts. Perfect for navigation bars, card footers, and responsive layouts.

<Tabs defaultValue="preview" class="h-auto">
    <TabsList variant="pills" slot="list">
        <TabsTrigger value="preview" variant="pills">Preview</TabsTrigger>
        <TabsTrigger value="code" variant="pills">Code</TabsTrigger>
    </TabsList>
    <TabsContent value="preview" preview class="space-y-4">
        <Row class="gap-0 p-4 border rounded bg-gray-50">
            <Button size="sm">Left</Button>
            <Spacer />
            <Button size="sm" variant="secondary">Right</Button>
        </Row>
        
        <Row class="gap-0 p-4 border rounded bg-gray-50">
            <Button size="sm">Start</Button>
            <Spacer />
            <Button size="sm" variant="outline">Middle</Button>
            <Spacer />
            <Button size="sm" variant="destructive">End</Button>
        </Row>

        <Column class="gap-0 h-24 p-4 border rounded bg-gray-50">
            <div class="text-sm font-medium">Header</div>
            <Spacer />
            <div class="text-sm text-gray-600">Footer</div>
        </Column>
    </TabsContent>
    <TabsContent value="code">
        <Code language="astro" code={`---
import Spacer from '@/components/ui/layout/Spacer.astro';
import { Row, Column } from '@/components/ui/layout';
import { Button } from '@/components/ui/forms';
---

{/* Horizontal spacing */}
<Row class="gap-0">
    <Button>Left</Button>
    <Spacer />
    <Button>Right</Button>
</Row>

{/* Multiple spacers */}
<Row class="gap-0">
    <Button>Start</Button>
    <Spacer />
    <Button>Middle</Button>
    <Spacer />
    <Button>End</Button>
</Row>

{/* Vertical spacing */}
<Column class="gap-0 h-24">
    <div>Header</div>
    <Spacer />
    <div>Footer</div>
</Column>`} />
    </TabsContent>
</Tabs>

## API Reference

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `class` | `string` | - | Additional CSS classes |
| `...rest` | `any` | - | All other HTML attributes and Alpine.js directives |