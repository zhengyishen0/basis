---
export interface Props {
  images?: Array<{
    src: string;
    alt?: string;
  }>;
  class?: string;
}

const { images = [], class: className = "" } = Astro.props;
---

<div class={`image-gallery ${className}`}>
  <p>Image Gallery component temporarily disabled for build fix</p>
</div>