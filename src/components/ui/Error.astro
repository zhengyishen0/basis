---
export interface Props {
  class?: string;
  [key: string]: any; // Allow any Alpine.js attributes
}

const { 
  class: className = "",
  ...rest
} = Astro.props;
---

<!-- 
TRUE Design System Error Component

Usage:
<Error x-show="todo.error" x-text="todo.error" />

<Error x-show="hasError">
  Something went wrong. Please try again.
</Error>
-->

<div class={`alert alert-error ${className}`} {...rest}>
  <slot />
</div>