---
// Import component styles
import '/src/styles/components/space.css';

export interface Props {
  class?: string;
  [key: string]: any;
}

const { 
  class: className = "",
  ...rest
} = Astro.props;
---

<!-- Space Component - Flexible spacer -->

<div class={`space ${className}`.trim()} {...rest}></div>